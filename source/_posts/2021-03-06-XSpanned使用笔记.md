---
title: XSpanned使用笔记
catalog: true
comments: true
indexing: true
header-img: ../../../../img/default.jpg
top: false
tocnum: true
date: 2021-01-20 10:56:46
subtitle:
tags:
- xlib
- Spanned
categories: Android
---

### 一.代码位置
- [Spanned](https://github.com/itemuse/XLib/blob/master/library/src/main/java/cn/xy/library/util/spannable/XSpanned.java)
此文是在此基础上做的讲解,用于加深印象


### 二.引用
   1. 将JitPack存储库添加到您的构建文件中
将其添加到存储库末尾的root build.gradle中：
```
allprojects {
      repositories {
      ...
      maven { url 'https://jitpack.io' }
      }
}
```

   2. 添加依赖项
```
dependencies {
      ...
      implementation 'com.github.itemuse:XLib:Tag'
}
```

   3. 初始化 Application中init
```
import cn.xy.library.XApp;
      ...
@Override
public void onCreate() {
      super.onCreate();
      XApp.init(this);
}
```

### 三.使用
   - #### **初始化**
把textview控件传过去即可
append传入要显示的字符
```
TextView mTextview = findViewById(R.id.text);
SpanUtils.with(mTextview)
        .append(getString(R.string.text)) 
        .create();
```

   - #### **设置字体大小**
```
.append("X")
.setFontSize(50)
.append("Y")
.setFontSize(150)
.append("Z")
.setFontSize(20)
```
{% asset_img Image1.png This is an example image %}


   - #### **前景色、背景色**
```
.setForegroundColor(getColor(R.color.colorAccent1))
```
{% asset_img Image2.png This is an example image %}
```
.setBackgroundColor(getColor(R.color.colorAccent1))
```
{% asset_img Image3.png This is an example image %}

   - #### **设置引用线颜色**
```
.setQuoteColor(getColor(R.color.colorAccent1))
```
{% asset_img Image4.png This is an example image %}

   - #### **设置缩进**
```
.setLeadingMargin(50, /*第一行缩进值*/ 0 /*其余行缩进值*/)
```
设置前
{% asset_img Image5.png This is an example image %}
设置后
{% asset_img Image6.png This is an example image %}

   - #### **设置行高**
```
/*
    public static final int ALIGN_BOTTOM   = 0;
    public static final int ALIGN_BASELINE = 1;
    public static final int ALIGN_CENTER   = 2;
    public static final int ALIGN_TOP      = 3;
*/
.setLineHeight(100/*行高*/,SpanUtils.ALIGN_CENTER/*重心*/)
```
设置前
{% asset_img Image7.png This is an example image %}
设置后
{% asset_img Image8.png This is an example image %}

   - #### **字体比例**
修改前：
```
.append(getString(R.string.text))
.append(getString(R.string.text))
.append(getString(R.string.text))
```
{% asset_img Image9.png This is an example image %}
修改后：
```
.append(getString(R.string.text))
.setFontProportion((float) 1.9) //整体比例
.append(getString(R.string.text))
.setFontXProportion((float) 1.5)//宽度比例
.append(getString(R.string.text))
.setFontXProportion((float) 0.3)//宽度比例
```
{% asset_img Image10.png This is an example image %}

   - #### **删除线、下划线**
```
.setStrikethrough() //删除线
```
{% asset_img Image11.png This is an example image %}
```
.setUnderline() //下划线
```
{% asset_img Image12.png This is an example image %}

   - #### **设置粗体、斜体**
```
.append(getString(R.string.text))
.setBold()           //设置粗体
.append(getString(R.string.text))
.setItalic()         //设置斜体
.append(getString(R.string.text))
.setBoldItalic()     //设置粗斜体
```
{% asset_img Image13.png This is an example image %}

   - #### **设置字体**
```
.append(getString(R.string.text))
.setTypeface(Typeface.DEFAULT_BOLD)       //设置字体
.append(getString(R.string.text))
.setTypeface(Typeface.SANS_SERIF)       //设置字体
.append(getString(R.string.text))
.setTypeface(Typeface.SERIF)       //设置字体
```
{% asset_img Image14.png This is an example image %}

   - #### **设置单独点击事件**
```
.append("点击事件1")
.setClickSpan(new ClickableSpan() {
    @Override
    public void onClick(View view) {
        XLog.i("//设置点击事件1");
    }
})      //设置点击事件
.append("点击事件2")
.setClickSpan(getColor(R.color.colorAccent4),true, new View.OnClickListener() {
    @Override
    public void onClick(View view) {
        XLog.i("//设置点击事件2");
    }
})      //设置点击事件
```
打印
```
2021-01-12 00:29:12.802 13577-13577/com.softwinner.app I/MainActivity.java: [ (MainActivity.java:75)#onClick ] //设置点击事件1
2021-01-12 00:29:13.190 13577-13577/com.softwinner.app I/MainActivity.java: [ (MainActivity.java:82)#onClick ] //设置点击事件2
```

   - #### **超链接**
```
.append(getString(R.string.text))
.setUrl("http://www.baidu.com")            //设置超链接
```
{% asset_img Image15.png This is an example image %}
点击会跳转浏览器
{% asset_img Image16.png This is an example image %}

   - #### **设置模糊**
```
.append(getString(R.string.text))
.setBlur((float) 1, BlurMaskFilter.Blur.SOLID)           //设置模糊
.append(getString(R.string.text))
.setBlur((float) 1, BlurMaskFilter.Blur.OUTER)           //设置模糊
.append(getString(R.string.text))
.setBlur((float) 1, BlurMaskFilter.Blur.INNER)           //设置模糊
.append(getString(R.string.text))
.setBlur((float) 1, BlurMaskFilter.Blur.NORMAL)           //设置模糊
```
{% asset_img Image17.png This is an example image %}

   - #### **设置阴影**
```
.append(getString(R.string.text))
.setShadow(15,5,5,getColor(R.color.colorAccent1))         //设置阴影
```
{% asset_img Image18.png This is an example image %}

   - #### **追加图片**
```
.append("追加图片")
.appendImage(R.drawable.ic_launcher_background,SpanUtils.ALIGN_BOTTOM)       //追加图片
```
{% asset_img Image19.png This is an example image %}

   - #### **追加空格**
```
.append("追加图片")
.appendSpace(20)       //追加空白
.append("追加图片")
```
{% asset_img Image20.png This is an example image %}